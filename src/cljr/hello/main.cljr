(ns hello.main
  (:require [clojure.data.json :as json]
            [medley.core :as medley]))

(defn one [] 1)

(defn -main []
  (json/pprint {"Hello" "World!"})
  (prn (medley/find-first even? (range 1 10))))
